# =============================================================================
# JWT Configuration (OBRIGATÓRIO)
# =============================================================================
# IMPORTANTE: Use um secret forte com pelo menos 32 caracteres
# Gerado usando: openssl rand -base64 48
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_SECRET_KEY_AT_LEAST_32_CHARS_LONG

# Issuer do token (quem emitiu o token) - OPCIONAL
JWT_ISSUER=go-data-jwt-example

# Tempo de expiração do access token - OPCIONAL
# Aceita segundos (3600) ou duration (1h, 30m, 24h)
# Padrão: 24h
JWT_EXPIRATION=3600

# Tempo de expiração do refresh token - OPCIONAL
# Deve ser maior que JWT_EXPIRATION
# Padrão: 168h (7 dias)
JWT_REFRESH_EXPIRATION=604800

# Algoritmo de assinatura - OPCIONAL
# Padrão: HS256
JWT_ALGORITHM=HS256

# =============================================================================
# Server Configuration
# =============================================================================
HOST=localhost
PORT=8080

# =============================================================================
# Database Configuration (Opcional - para jwt_banco example)
# =============================================================================
# DB_PROVIDER=postgresql
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=godata
# DB_USER=postgres
# DB_PASSWORD=secret123

# =============================================================================
# Exemplo de geração de secret seguro
# =============================================================================
# $ openssl rand -base64 48
# Ou use: https://1password.com/password-generator/

# =============================================================================
# Nota de Segurança
# =============================================================================
# 1. NUNCA comite o arquivo .env no Git
# 2. Copie este arquivo para .env e configure seu JWT_SECRET
# 3. JWT_SECRET deve ter no mínimo 32 caracteres (recomendado 48+)
# 4. Use secrets diferentes para desenvolvimento e produção
# 5. Rotacione o secret periodicamente em produção
# 6. Em produção, use secrets manager (AWS Secrets Manager, Azure Key Vault, HashiCorp Vault)
