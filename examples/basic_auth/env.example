# Configurações do Banco de Dados
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_secure_password_here
DB_NAME=odata_test

# Configurações do Servidor
SERVER_HOST=localhost
SERVER_PORT=3000

# Nota de Segurança:
# 1. NUNCA comite o arquivo .env no Git
# 2. Copie este arquivo para .env e configure suas credenciais
# 3. Use senhas fortes para DB_PASSWORD em produção
# 4. As senhas dos usuários de teste (admin123, user123) são apenas para desenvolvimento
# 5. Em produção, gere senhas seguras e altere-as imediatamente após o primeiro login

